<div class="ticker">
  <div class="ticker__track">
    <% (Storefront::TICKER * 2).each do |claim| %>
      <span class="ticker__item"><%= cj_icon "zap" %><%= claim %></span>
    <% end %>
  </div>
</div>
