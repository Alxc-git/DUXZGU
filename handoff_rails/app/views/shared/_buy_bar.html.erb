<div class="buy-bar">
  <span class="buy-bar__price">
    <b><%= Storefront::PRICE %></b>
    <span>130 gummies · 32 servings</span>
  </span>
  <%= link_to checkout_path(flavor: flavor.slug), class: "btn btn--primary" do %>
    <%= cj_icon "shopping-cart", size: "1.15em" %> Add to cart
  <% end %>
</div>
